# Local Windows data-prep. CosyVoice training itself stays on DGX Spark.
# Prefer Python 3.10 if FunASR wheels fail on newer CPython.
imageio-ffmpeg>=0.5.1
numpy>=1.24
soundfile>=0.12.1
tqdm>=4.66
torch>=2.2
# Fun-ASR-Nano needs Python 3.10 + CUDA. Use this on DGX Spark, not Win 3.14.
funasr>=1.2
modelscope>=1.20
silero-vad>=5.1
# Used automatically if FunASR (numpy<2) cannot install, e.g. Python 3.14.
faster-whisper>=1.1
